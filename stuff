# to clone a repo without needing to enter password all the time;
git clone https://token_code@repo_name

# to start vagrant;
cd into home directory (cd ~)
vagrant ssh

# before shutting your pc use;
vagrant halt

# to see the root files
ls /

# to enter the root files
cd /

# to go to home
cd ~

